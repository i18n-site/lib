#!/usr/bin/env coffee

> @3-/urle > urld
  @3-/intbin > binU64
#   @3-/uridir
#   path > join

# ROOT = uridir(import.meta)
console.log binU64 urld '5nq'
# console.log urle new Uint8Array [ 1, 3, 123]
