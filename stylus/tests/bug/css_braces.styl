:root {
  --bg-color: #f8fafc;
  --card-bg: #ffffff;
}

body,
input,
select,
button,
textarea {
  font-family:
    t,
    -apple-system,
    BlinkMacSystemFont;
}

@media (max-width: 768px) {
  header {
    flex-direction: column;
    gap: 16px;
  }
}
